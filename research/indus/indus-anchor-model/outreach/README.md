# Outreach Materials

This folder contains public-facing outreach documents for the ICIT/Fuls validation request.

## H24 Notice

**Email drafts are NOT stored here.** Per governance rule H24, all private correspondence (email drafts, contact details, follow-up notes) is stored exclusively in `.correspondence/` at the repository root, which is gitignored and never pushed to the public repository.

If you are looking for the Fuls outreach email drafts, they are in:
```
.correspondence/fuls_outreach.md   (local only, gitignored)
```

## What IS in this folder

- `icit_package_manifest.md` — the 7-item package to send to ICIT/Fuls with the initial contact

## What to send

See `icit_package_manifest.md` for the exact list of materials to attach. The two-page summary is at `docs/two_page_summary.md`. The ICIT validation plan is at `docs/icit_validation_plan.md`.
