# macOS Services

This directory will contain macOS-specific startup and service assets.

Planned contents:
- LaunchAgent or LaunchDaemon templates
- install/uninstall helpers
- login/startup documentation
- environment/config examples

Required behavior:
- backend can be started and managed in a documented macOS-native way
- tray behavior is supported and documented
- startup/login expectations are explicit
