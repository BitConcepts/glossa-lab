# Windows Services

This directory will contain Windows-specific startup and service assets.

Planned contents:
- startup registration scripts
- service wrapper or manager config
- tray startup integration
- install/uninstall scripts
- documentation

Required behavior:
- tray starts automatically on Windows login/startup
- tray can open or control background services
