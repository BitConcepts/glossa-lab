# Tray

This directory will contain the tray application.

Planned responsibilities:
- quick status
- open UI
- service controls
- startup integration where supported

Windows is expected to be the first-class tray target.
Linux tray support must be documented per desktop/runtime constraints.
