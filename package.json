{
  "name": "glossa-lab",
  "private": true,
  "description": "Glossa Lab — monorepo root. Frontend workspace in frontend/, backend in backend/.",
  "workspaces": ["frontend"],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "cd frontend && npm run build",
    "test:e2e": "cd frontend && npx playwright test"
  },
  "devDependencies": {
    "vite": "*",
    "react": "*",
    "@playwright/test": "*"
  }
}
