# Phase-30 — Dashboard-driven research synthesis
## Inputs
- 136 discovery items (status=new) in the database after a fresh fetch + mine cycle
  on 2026-05-04. 15 items have a fetch timestamp in the last 14 days; remaining
  rows are pre-existing.
- 15 studies registered in the project DB.
- All Phase-21 → Phase-29 graph experiments + reports already on disk
  (`reports/phase{21..29}*.json`).
- Latest verdict (`reports/phase29f_verdict.json`) recommends Phase-30 priorities
  centered on **re-running Phase-25c period stratification, Phase-25e
  Shu-ilishu filter, and Phase-27c iconographic anchor score against the
  Mahadevan 1977 corpus** for 9.4× statistical power.

## AI insight (auto-generated by /api/v1/dashboard/insight)
- **What it means** — *"These findings suggest a sophisticated, self-organized
  Harappan civilization with advanced trade networks, astronomical knowledge,
  and complex administrative systems."*
- **Top highlights**
    1. arxiv:2604.23582 — *Scale-Free Commercial Network in the IVC: power-law
       analysis of Harappan seal data* (`α≈2.3-2.6`, KS-confirmed, exponential
       ruled out).
    2. arxiv:1310.6474 — *Possible Harappan Astronomical Observatory at
       Dholavira* (proposes strategic structure for astronomical observations).
    3. osf:h8ct7 — *Ancient tax tokens, trade licenses and metrological records*
       (Mukhopadhyay, 2021): inscribed Indus objects as administrative tools
       encoding a commercial sublanguage.
- **Suggested next actions**
    1. Re-run Phase-25c against the Mahadevan corpus.
    2. Save and follow up on the Harappan seal distribution analysis.
    3. Investigate other potential astronomical sites in the Indus Valley.
    4. Explore further the administrative functions of inscribed objects.
    5. Update the Indus Structural Atlas with new findings.

## Impact on existing studies / experiments
The AI's `impact` array came back empty for this run because the dashboard's
`discover_experiments()` call returned 0 entries (registry timing issue,
tracked in TODO list). Manual triage:
- The **Mukhopadhyay tax-token thesis** (3 items in the feed: scale-free
  commercial network, gemstone fish-signs, gold-assaying needles) directly
  reinforces the *administrative-vs-linguistic stratification* hypothesis
  Phase-20 already tested via length bins. **Action**: feed the Mukhopadhyay
  papers' fish-sign + crucible-blowpipe sign anchor proposals into a new
  iconographic-anchors run (Phase-30c, see below).
- The **scale-free seal/site distribution finding** is independent of script
  decipherment but dovetails with our spatial stratification (`Phase-26d
  cisi_findspot`). **Action**: Phase-30d could re-run findspot analysis with
  the explicit scale-free hypothesis as a null competitor.
- The **Harappan Observatory paper** (Vahia & Menon 2013) is older but newly
  surfaced. Adds astronomical-marker class to potential iconographic anchors;
  not directly script-related.

## Phase-30 graph experiments (sketch — to be implemented as graph JSONs)
Each below maps cleanly to the existing `experiment_graph_phase{21..29}.py`
atomic-node module pattern. None require *new* atomic nodes — they swap the
corpus loader to `M77InscriptionLoader` (already wired in Phase-29a).

* `indus_phase30a_period_stratified_m77.json` — re-run Phase-25c period
  stratification against the Mahadevan 1977 corpus (1,669 inscriptions) instead
  of the 178-inscription CISI subset. Expected: 9.4× more statistical power per
  bucket; test whether period-2 vs period-3 inscriptions show distinct
  signatures.
* `indus_phase30b_shu_ilishu_filter_m77.json` — re-run Phase-25e Shu-ilishu
  filter against the larger M77 corpus + ePSD2 PN list (4,848 entries).
* `indus_phase30c_iconographic_anchors_m77.json` — re-run Phase-27c
  iconographic-anchor score with anchors seeded from the Mukhopadhyay
  fish-sign / gold-assaying-needle proposals (highlighted in the AI synthesis
  above) against M77.
* `indus_phase30d_scalefree_findspot_null.json` — extend Phase-26d findspot
  analysis with the Mahesh 2026 scale-free model as an explicit competing null
  hypothesis. Tests whether *site-level* sign-frequency power-law fits beat
  exponential alternatives (mirroring the seal-attribute finding).
* `indus_phase30e_yajnadevam_falsification.json` — Phase-29 verdict
  priority 5: Yajnadevam (2024) Sanskrit decipherment as a competing phoneme
  map, scored against IconographicAnchorScore. Clean falsification round.
* `indus_phase30f_verdict.json` — Phase-30 verdict synthesizing 30a-e against
  Phase-29 baselines.

## Status of this Phase-30 batch
- ✅ Dashboard live with AI insight + impact-aware next-actions.
- ✅ Auto Discovery panel live in Settings (manual fetch+mine, source key
  config, scheduler toggle, recipient summary).
- ✅ Manual fetch+mine reachable from THREE surfaces: Discovery view (▶ Run
  fetch / ✨ Run mine + ⚙ Options for source/topic/since/limit), Settings →
  Auto Discovery (Fetch now / Mine now (50)), and CLI
  (`python -m glossa_lab.discovery {fetch,mine,daily} --notify`).
- ✅ Discovery DB populated with 136 mined items (study=majority, hypothesis,
  finding) covering scale-free seal networks, tax-token Mukhopadhyay
  hypotheses, Harappan astronomy, urban hydraulics, Rakhigarhi
  bioarchaeology, fortification analysis.
- ⏳ Phase-30a → 30f graph JSONs not yet built (sketches above). Each requires
  ~30-90 minutes to wire + run; collectively a multi-session effort.
- ⏳ The atomic-node implementations my earlier (renumbered) phases 22-27
  proposed (`ClusterCollapseTransform`, `IconographicAnchorPin`,
  `ShuffledLMNullDistribution`, `JointLMPosterior`,
  `KFoldCorpusSplitter/Aggregator`) remain unbuilt. They were originally
  proposed as numerical phases 21-27 but the project's existing phase numbers
  go up to 29 — to avoid collision, future work should be tagged Phase-30+ as
  in this report.
- ⏳ `n_experiments: 0` reported by dashboard despite 50+ registered graphs.
  Symptom of `discover_experiments()` being called before the lifespan has
  populated the registry; one-line fix in `api/dashboard.py` (await the
  registry-ready event or call `register_graph_experiments()` lazily).

## Recommendation
Treat the discovery items as a curated short-list and run **Phase-30a first**:
period-stratified Mahadevan corpus is the highest-leverage, lowest-risk
deliverable from the Phase-29 verdict (no external data acquisition needed,
the corpus is already wired). The Mukhopadhyay-anchor experiment (30c) is the
next-highest-value because the new discovery items provide concrete anchor
proposals. Defer 30b/d/e until 30a/c results are known.

---
Generated 2026-05-04 from `/api/v1/dashboard/highlights?include_ai=true`.
